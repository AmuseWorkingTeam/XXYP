<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!--<item name="windowBackground">@color/c1</item>-->
    </style>

    <style name="ActivityAnimation" parent="@style/AppTheme">
        <item name="android:windowAnimationStyle">@style/activity_animation</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Transparent" parent="@style/ActivityAnimation">
        <!--将Activity的Theme设置成透明-->
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="activity_animation">
        <item name="android:activityOpenEnterAnimation">@anim/activity_open_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/activity_open_exit</item>
        <item name="android:activityCloseEnterAnimation">@anim/out_left_in</item>
        <item name="android:activityCloseExitAnimation">@anim/out_left_out</item>
        <item name="android:taskOpenEnterAnimation">@null</item>
        <item name="android:taskOpenExitAnimation">@null</item>
        <item name="android:taskCloseEnterAnimation">@null</item>
        <item name="android:taskCloseExitAnimation">@null</item>
        <item name="android:taskToFrontEnterAnimation">@null</item>
        <item name="android:taskToFrontExitAnimation">@null</item>
        <item name="android:taskToBackEnterAnimation">@null</item>
        <item name="android:taskToBackExitAnimation">@null</item>
    </style>

    <!--加载框style-->
    <style name="progressbar_loading" parent="@android:style/Widget.ProgressBar">
        <item name="android:indeterminateDrawable">@anim/loading</item>
    </style>

    <!--dialog style-->
    <style name="dialog_normal" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!--相册-->
    <!-- POPUP -->

    <style name="AlbumPopupAnimation" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/album_popup_in</item>
        <item name="android:windowExitAnimation">@anim/album_popup_out</item>
    </style>

    <style name="Theme.TMessages" parent="@android:style/Theme.Holo.Light">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBarOverlay">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorBackground">@android:color/white</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:itemTextAppearance">@style/ActionBar.Transparent.TMessages.Item</item>
        <item name="android:listViewStyle">@style/Theme.TMessages.ListView</item>
        <item name="android:listChoiceBackgroundIndicator">@drawable/album_list_selector</item>
        <item name="android:actionBarItemBackground">@drawable/album_bar_selector_style</item>
    </style>

    <!--ACTION BAR ITEMS-->

    <style name="ActionBar.Transparent.TMessages.Item" parent="android:TextAppearance.Widget.IconMenu.Item">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">18sp</item>
    </style>

    <!--LIST VIEW-->

    <style name="Theme.TMessages.ListView" parent="@android:style/Widget.ListView">
        <item name="android:listSelector">@drawable/album_list_selector</item>
        <item name="android:fadingEdge">none</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:divider">#dcdcdc</item>
    </style>

    <style name="chat_progressbar_loading" parent="@android:style/Widget.ProgressBar.Small">
        <item name="android:indeterminateDrawable">@anim/chat_send_message_anim</item>
    </style>

</resources>
